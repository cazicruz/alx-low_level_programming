#include "main.h"

/**
 * main - Entry point
 * Return: always return 0 (success)
 */

void print_alphabet(void);

int main(void)
{
	return (0)
}
void print_alphabet(void)
{
	char ch;

	for(ch = 97; ch <= 122; ch++)
	{
		_putchar(ch);
	}
	_putchar("\n");
}
