#include <stdio.h>

/**
 * main - Entry point
 * Return: always return 0 (success)
 */

void print_alphabet(void);

int main(void)
{
	return (0)
}
void print_alphabet(void)
{
	char ch;

	for(ch = "a"; ch <= "z"; ch++)
	{
		printf("%c", ch);
	}
	printf("\n");
}
