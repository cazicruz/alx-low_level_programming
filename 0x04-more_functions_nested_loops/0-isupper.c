#include <stdio.h>
#include "main.h"

/*
 * _isupper - starting point
 * Returning: return 0 if lowercase and 1 if uppercase
 */

int _isupper(int c)
{
	if (a >= 'A' && <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
