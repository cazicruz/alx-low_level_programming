#include "main.h"

/**
 * @c: inpute parameter
 * _isupper -> starting point
 * Returning: return 0 if lowercase
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
