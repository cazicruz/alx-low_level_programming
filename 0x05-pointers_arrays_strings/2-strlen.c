#include <stdio.h>
#include "main.h"

/**
 * _strlen - Entry point
 * Discription: this is a function that swaps two values
 * @a: pointer variable
 * @b: pointer variable
 */

int _strlen(char *s)
{
	return (sizeof(s)-1);
}
