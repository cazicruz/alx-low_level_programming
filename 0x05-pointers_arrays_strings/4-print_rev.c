#include <stdio.h>
#include "main.h"

/**
 * _puts - Entry point
 * @str: string
 * Return: returns the lenght of the string
 */


void print_rev(char *s)
{
	int count = 0;
	int n;

	while (str[count] != 0)
	{
		count++;
	}
	n = count;
	for (; n <= count; n--)
	{
		_putchar(s[n]);
	}
	_putchar('\n');
}
