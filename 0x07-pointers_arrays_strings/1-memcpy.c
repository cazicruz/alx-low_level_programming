#include "main.h"

/**
 * _memcpy - Entry point
 * @s: memory area
 * @n: number of bytes to fill
 * @b: constant byte
 * Return: return *s
 */

char *_memcpy(char *dest, char *src, unsigned int n);
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		dest[i] = src[i];
	}
	return (s);
}
